# -*- coding: utf-8 -*-

# auth_portal_app/lib/usage_guide/guide_image.py

# ============================================================

# PAIS使い方ガイド：画像

#
# 機能：
#
# - AIによる画像生成の利用目的を定義する
# - AIによる画像修正の利用目的を定義する
# - 画像生成・画像修正の利用履歴の確認方法を定義する
#
# 方針：
#
# - Streamlit描画処理は持たない
# - 説明内容だけを定義する
#
# ============================================================

from __future__ import annotations

# ============================================================
# imports
# ============================================================

from .models import (
    GuideCategory,
    GuideItem,
    GuideSection,
    GuideSubSection,
)

# ============================================================
#
# ＜＜＜＜＜＜　画像生成　＞＞＞＞＞＞
#
# ============================================================

ITEM_IMAGE_GENERATION = GuideItem(
    key="image_generation",
    title="AIを使って画像を作成したい",
    summary="作成したい画像の内容を文章で指示し，AIで画像を生成します．",
    uses_ai=True,
    sections=(

        # --------------------------------------------------------
        # 概要
        # --------------------------------------------------------

        GuideSection(
            title="概要",
            body="""
作成したい画像の内容を文章で指示し，AIを使って画像を生成します．

生成した画像に対して追加の指示を行い，
続けて画像を修正することもできます．
""",
        ),

        # --------------------------------------------------------
        # 基本的な流れ
        # --------------------------------------------------------

        GuideSection(
            title="基本的な流れ",
            body="""
<div class="guide-point">
<span class="guide-route">画像の内容を指示 ➡ AIによる画像生成 ➡ 必要に応じて画像を修正</span><br>
</div>
""",

            subsections=(

                # ------------------------------------------------
                # ① 作成したい画像の内容を入力する
                # ------------------------------------------------

                GuideSubSection(
                    title="① 作成したい画像の内容を入力する",
                    body="""
作成したい画像の内容を【生成プロンプト】に入力します．

<div class="guide-note">
<span class="guide-route">ポータル ▶ イメージメーカー ▶ 画像生成</span><br>
</div>
""",
                ),

                # ------------------------------------------------
                # ② 画像を生成する
                # ------------------------------------------------

                GuideSubSection(
                    title="② 画像を生成する",
                    body="""
【生成する】を押して，AIで画像を生成します．
""",
                ),

                # ------------------------------------------------
                # ③ 必要に応じて画像を修正する
                # ------------------------------------------------

                GuideSubSection(
                    title="③ 必要に応じて画像を修正する",
                    body="""
生成された画像を修正したい場合は，
修正したい内容を入力して【修正版を生成する】を押します．

修正後の画像に対して，さらに修正を繰り返すこともできます．
""",
                ),

                # ------------------------------------------------
                # ④ 画像を保存する
                # ------------------------------------------------

                GuideSubSection(
                    title="④ 画像を保存する",
                    body="""
完成した画像をPNGファイルとしてダウンロードします．
画像をInboxへ保存することもできます．
""",
                ),

                # ------------------------------------------------
                # ⑤ 作成結果を確認する
                # ------------------------------------------------

                GuideSubSection(
                    title="⑤ 作成結果を確認する",
                    body="""
<div class="guide-red">
担当者は主に次の作業を行います．

- 生成された画像が意図した内容になっているか確認する．
- 必要に応じて修正内容を指示し，再度画像を生成する．
- 最終的に使用する画像を確認して保存する．
</div>

<div class="guide-warning">
AIが生成する画像は，入力した指示を完全に反映するとは限りません．
文字，人物，物体の形状などに不自然な部分がないか確認してください．
</div>
""",
                ),

                # subsectionsのクローズ
            ),

            # GuideSectionのクローズ
        ),

        # sectionsのクローズ
    ),

    # GuideItemのクローズ
)


# ============================================================
#
# ＜＜＜＜＜＜　画像修正　＞＞＞＞＞＞
#
# ============================================================

ITEM_IMAGE_EDIT = GuideItem(
    key="image_edit",
    title="AIを使って既存の画像を修正したい",
    summary="画像を設定し，修正したい内容を文章で指示してAIで画像を修正します．",
    uses_ai=True,
    sections=(

        # --------------------------------------------------------
        # 概要
        # --------------------------------------------------------

        GuideSection(
            title="概要",
            body="""
既存の画像をもとに，修正したい内容を文章で指示してAIで画像を修正します．

修正後の画像に対して，さらに修正を繰り返すこともできます．
""",
        ),

        # --------------------------------------------------------
        # 基本的な流れ
        # --------------------------------------------------------

        GuideSection(
            title="基本的な流れ",
            body="""
<div class="guide-point">
<span class="guide-route">元画像 ➡ 修正内容を指示 ➡ AIによる画像修正 ➡ 修正画像</span><br>
</div>
""",

            subsections=(

                # ------------------------------------------------
                # ① 修正する画像を設定する
                # ------------------------------------------------

                GuideSubSection(
                    title="① 修正する画像を設定する",
                    body="""
修正する画像をアップロードするか，Inboxから画像を選択して設定します．

<div class="guide-note">
<span class="guide-route">ポータル ▶ イメージメーカー ▶ 画像修正</span><br>
</div>
""",
                ),

                # ------------------------------------------------
                # ② 修正内容を入力する
                # ------------------------------------------------

                GuideSubSection(
                    title="② 修正内容を入力する",
                    body="""
画像のどこをどのように変更したいかを【修正内容】に入力します．
""",
                ),

                # ------------------------------------------------
                # ③ 修正版を生成する
                # ------------------------------------------------

                GuideSubSection(
                    title="③ 修正版を生成する",
                    body="""
【修正版を生成する】を押して，AIで画像を修正します．

必要に応じて，新しい修正内容を入力して修正を繰り返します．
""",
                ),

                # ------------------------------------------------
                # ④ 修正した画像を保存する
                # ------------------------------------------------

                GuideSubSection(
                    title="④ 修正した画像を保存する",
                    body="""
完成した画像をPNGファイルとしてダウンロードします．
画像をInboxへ保存することもできます．
""",
                ),

                # ------------------------------------------------
                # ⑤ 修正結果を確認する
                # ------------------------------------------------

                GuideSubSection(
                    title="⑤ 修正結果を確認する",
                    body="""
<div class="guide-red">
担当者は主に次の作業を行います．

- 修正内容が意図どおりに反映されているか確認する．
- 修正によって不要な変化が生じていないか確認する．
- 必要に応じて追加の修正を行う．
</div>

<div class="guide-warning">
AIによる画像修正では，指定していない部分まで変化する場合があります．
修正後の画像全体を確認してください．
</div>
""",
                ),

                # subsectionsのクローズ
            ),

            # GuideSectionのクローズ
        ),

        # sectionsのクローズ
    ),

    # GuideItemのクローズ
)


# ============================================================
#
# ＜＜＜＜＜＜　利用履歴　＞＞＞＞＞＞
#
# ============================================================

ITEM_IMAGE_USAGE_HISTORY = GuideItem(
    key="image_usage_history",
    title="画像生成・画像修正の利用履歴を確認したい",
    summary="自分が行った画像生成・画像修正の利用状況を確認します．",
    uses_ai=False,
    sections=(

        # --------------------------------------------------------
        # 概要
        # --------------------------------------------------------

        GuideSection(
            title="概要",
            body="""
画像生成と画像修正について，自分の利用履歴を確認できます．

期間ごとの利用件数や，月別・モデル別の利用状況を確認することができます．
""",
        ),

        # --------------------------------------------------------
        # 基本的な流れ
        # --------------------------------------------------------

        GuideSection(
            title="基本的な流れ",
            body="""
<div class="guide-point">
<span class="guide-route">利用履歴 ➡ 期間を指定 ➡ 利用状況を確認</span><br>
</div>
""",

            subsections=(

                # ------------------------------------------------
                # ① 利用履歴を開く
                # ------------------------------------------------

                GuideSubSection(
                    title="① 利用履歴を開く",
                    body="""
【利用履歴】を開きます．

<div class="guide-note">
<span class="guide-route">ポータル ▶ イメージメーカー ▶ 利用履歴</span><br>
</div>
""",
                ),

                # ------------------------------------------------
                # ② 確認する期間を指定する
                # ------------------------------------------------

                GuideSubSection(
                    title="② 確認する期間を指定する",
                    body="""
確認したい年月や開始日・終了日を指定します．
""",
                ),

                # ------------------------------------------------
                # ③ 利用状況を確認する
                # ------------------------------------------------

                GuideSubSection(
                    title="③ 利用状況を確認する",
                    body="""
画像生成と画像修正の件数を確認します．

月別の利用状況や，月別・モデル別の利用状況も確認できます．
必要に応じて，集計結果をCSVファイルとしてダウンロードします．
""",
                ),

                # subsectionsのクローズ
            ),

            # GuideSectionのクローズ
        ),

        # sectionsのクローズ
    ),

    # GuideItemのクローズ
)


# ============================================================
# 大項目：画像
# ============================================================

GUIDE_IMAGE = GuideCategory(
    key="image",
    title="画像の処理",
    icon="🎨",
    summary="AIによる画像生成・画像修正や，利用履歴の確認を行います．",
    items=(
        ITEM_IMAGE_GENERATION,
        ITEM_IMAGE_EDIT,
        ITEM_IMAGE_USAGE_HISTORY,
    ),
)
