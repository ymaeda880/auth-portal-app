# -*- coding: utf-8 -*-

# auth_portal_app/lib/usage_guide/guide_report_search.py
# ============================================================
# PAIS使い方ガイド：報告書検索
#
# 機能：
# - RAGを利用した報告書検索の説明を管理する
# - キーワード検索の説明を管理する
# - 報告書閲覧の説明を管理する
# ============================================================

from __future__ import annotations

# ============================================================
# imports
# ============================================================

from .models import (
    GuideCategory,
    GuideItem,
    GuideSection,
)


# ============================================================
# RAG検索
# ============================================================

ITEM_RAG_SEARCH = GuideItem(
    key="rag_search",
    title="過去の報告書について自由に質問したい",
    summary="過去の報告書を対象として，文章で質問して情報を探します．",
    page_name="RAG検索",
    uses_ai=True,
    sections=(
        GuideSection(
            title="概要",
            body="""
PAISに登録されている過去の報告書を対象として，
自然な文章で質問し，関連する報告書や内容を検索します．

単純なキーワード一致だけではなく，
質問内容に関連する報告書の情報を探したい場合に使用します．
""",
        ),
        GuideSection(
            title="質問例",
            body="""
例えば，次のような質問ができます．

- 「北海道に関するプロジェクト（報告書）にどのようなものがあるか教えて」
- 「鳥類に関する調査を行ったプロジェクト（報告書）にどのようなものがあるか教えて」
- 「鳥類に関する調査を行ったプロジェクト（報告書）について，どのような調査を行ったことがあるか教えて」
""",
        ),
        GuideSection(
            title="注意事項",
            body="""
AIによる回答は，検索された報告書の内容をもとに作成されますが，
回答内容が完全に正しいとは限りません．

重要な内容については，元の報告書も確認してください．
""",
        ),
    ),
)


# ============================================================
# キーワード検索
# ============================================================

ITEM_KEYWORD_SEARCH = GuideItem(
    key="keyword_search",
    title="特定のキーワードを含む報告書を検索したい",
    summary="指定した言葉を含む報告書を検索します．",
    page_name="キーワード検索",
    uses_ai=False,
    sections=(
        GuideSection(
            title="概要",
            body="""
特定の言葉が含まれている報告書を検索します．

検索したい言葉が明確な場合に利用します．
""",
        ),
        GuideSection(
            title="検索例",
            body="""
例えば，

「環境アセスメント」

という言葉を含む報告書を検索できます．
""",
        ),
    ),
)


# ============================================================
# 報告書閲覧
# ============================================================

ITEM_REPORT_VIEWER = GuideItem(
    key="report_viewer",
    title="過去の報告書を閲覧したい",
    summary="PAISに登録されている報告書を確認します．",
    page_name="報告書閲覧",
    uses_ai=False,
    sections=(
        GuideSection(
            title="概要",
            body="""
PAISに登録されている過去の報告書を探し，
内容を閲覧するために使用します．
""",
        ),
    ),
)


# ============================================================
# 大項目：報告書検索
# ============================================================

GUIDE_REPORT_SEARCH = GuideCategory(
    key="report_search",
    title="報告書検索",
    icon="🔎",
    summary="過去の報告書を質問，キーワードなどから検索・閲覧します．",
    items=(
        ITEM_RAG_SEARCH,
        ITEM_KEYWORD_SEARCH,
        ITEM_REPORT_VIEWER,
    ),
)