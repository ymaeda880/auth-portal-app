# -*- coding: utf-8 -*-

# auth_portal_app/lib/usage_guide/guide_document_creation.py

# ============================================================

# PAIS使い方ガイド：文書・資料の作成

#
# 機能：
#
# - AIによる文章作成の利用目的を定義する
# - 表作成の利用目的を定義する
# - PowerPointスライド作成の利用目的を定義する
#
# 方針：
#
# - Streamlit描画処理は持たない
# - 説明内容だけを定義する
#
# ============================================================

from __future__ import annotations

# ============================================================
# imports
# ============================================================

from .models import (
    GuideCategory,
    GuideItem,
    GuideSection,
    GuideSubSection,
)

# ============================================================
#
# ＜＜＜＜＜＜　AI文章作成　＞＞＞＞＞＞
#
# ============================================================

ITEM_AI_WRITING = GuideItem(
    key="ai_writing",
    title="AIを使って文章を作成したい",
    summary="元となる文章やメモをもとに，メール文や報告書などの文章をAIで作成します．",
    uses_ai=True,
    sections=(

        # --------------------------------------------------------
        # 概要
        # --------------------------------------------------------

        GuideSection(
            title="概要",
            body="""
元となる文章やメモをもとに，AIを使って文章を作成します．

メール文，報告書，議事メモなど，用途に応じた文章の作成に利用できます．
""",
        ),

        # --------------------------------------------------------
        # 基本的な流れ
        # --------------------------------------------------------

        GuideSection(
            title="基本的な流れ",
            body="""
<div class="guide-point">
<span class="guide-route">元となる文章・メモ ➡ 用途を指定 ➡ AIによる文章作成 ➡ 作成結果</span><br>
</div>
""",

            subsections=(

                # ------------------------------------------------
                # ① 元となる文章を設定する
                # ------------------------------------------------

                GuideSubSection(
                    title="① 元となる文章を設定する",
                    body="""
元となる文章やメモを直接入力するか，ファイルを設定します．

<div class="guide-note">
<span class="guide-route">ポータル ▶ テキストスタジオ ▶ AI文章作成</span><br>
</div>
""",
                ),

                # ------------------------------------------------
                # ② 作成する文章の用途を指定する
                # ------------------------------------------------

                GuideSubSection(
                    title="② 作成する文章の用途を指定する",
                    body="""
作成する文章の用途を選択します．

メール文，報告書，議事メモなどから，
作成したい文章に合ったものを選択します．

必要に応じて，文章の長さや表現などについて追加の指示を入力します．
""",
                ),

                # ------------------------------------------------
                # ③ 文章を作成する
                # ------------------------------------------------

                GuideSubSection(
                    title="③ 文章を作成する",
                    body="""
【文章生成】を押して，AIで文章を作成します．
""",
                ),

                # ------------------------------------------------
                # ④ 作成結果を確認する
                # ------------------------------------------------

                GuideSubSection(
                    title="④ 作成結果を確認する",
                    body="""
<div class="guide-red">
担当者は主に次の作業を行います．

- 作成された文章が目的に合っているか確認する．
- 元となる文章にない事実が追加されていないか確認する．
- 表現や内容を確認し，必要に応じて修正する．
</div>

<div class="guide-warning">
AIが作成した文章には，誤りや不適切な表現が含まれる可能性があります．
そのまま使用せず，最終的な内容を必ず確認してください．
</div>
""",
                ),

                # subsectionsのクローズ
            ),

            # GuideSectionのクローズ
        ),

        # sectionsのクローズ
    ),

    # GuideItemのクローズ
)


# ============================================================
#
# ＜＜＜＜＜＜　表作成　＞＞＞＞＞＞
#
# ============================================================

ITEM_TABLE_CREATION = GuideItem(
    key="table_creation",
    title="Wordで使用する表を作成したい",
    summary="Excelなどからコピーした表を整え，WordやHTMLで使用できる表を作成します．",
    uses_ai=False,
    sections=(

        # --------------------------------------------------------
        # 概要
        # --------------------------------------------------------

        GuideSection(
            title="概要",
            body="""
Excelなどからコピーした表を貼り付け，
レイアウトやスタイルを指定して表を作成します．

作成した表はWordまたはHTML形式で保存できます．
""",
        ),

        # --------------------------------------------------------
        # 基本的な流れ
        # --------------------------------------------------------

        GuideSection(
            title="基本的な流れ",
            body="""
<div class="guide-point">
<span class="guide-route">Excelなどの表 ➡ レイアウト・スタイルを設定 ➡ Word・HTMLの表</span><br>
</div>
""",

            subsections=(

                # ------------------------------------------------
                # ① 表を貼り付ける
                # ------------------------------------------------

                GuideSubSection(
                    title="① 表を貼り付ける",
                    body="""
Excelなどからコピーした表を貼り付けます．

<div class="guide-note">
<span class="guide-route">ポータル ▶ テキストスタジオ ▶ 表作成</span><br>
</div>
""",
                ),

                # ------------------------------------------------
                # ② 表の設定を行う
                # ------------------------------------------------

                GuideSubSection(
                    title="② 表の設定を行う",
                    body="""
ヘッダー，表の幅，スタイル，列幅などを設定します．

必要に応じて，表の下に表示する注も設定します．
""",
                ),

                # ------------------------------------------------
                # ③ 表を作成する
                # ------------------------------------------------

                GuideSubSection(
                    title="③ 表を作成する",
                    body="""
【表作成】を押して表を作成し，画面上で作成結果を確認します．
""",
                ),

                # ------------------------------------------------
                # ④ 作成した表を保存する
                # ------------------------------------------------

                GuideSubSection(
                    title="④ 作成した表を保存する",
                    body="""
作成した表をWordまたはHTML形式でダウンロードします．
""",
                ),

                # ------------------------------------------------
                # ⑤ 作成結果を確認する
                # ------------------------------------------------

                GuideSubSection(
                    title="⑤ 作成結果を確認する",
                    body="""
<div class="guide-red">
担当者は主に次の作業を行います．

- 表の内容が正しく表示されているか確認する．
- ヘッダー，列幅，セルの結合などが意図どおりになっているか確認する．
- 必要に応じて設定を変更して表を作り直す．
</div>
""",
                ),

                # subsectionsのクローズ
            ),

            # GuideSectionのクローズ
        ),

        # sectionsのクローズ
    ),

    # GuideItemのクローズ
)


# ============================================================
#
# ＜＜＜＜＜＜　PowerPointスライド作成　＞＞＞＞＞＞
#
# ============================================================

ITEM_SLIDE_CREATION = GuideItem(
    key="slide_creation",
    title="PowerPointスライドを作成したい",
    summary="SlideTex形式で記述した内容からPowerPointスライドを作成します．",
    uses_ai=False,
    sections=(

        # --------------------------------------------------------
        # 概要
        # --------------------------------------------------------

        GuideSection(
            title="概要",
            body="""
SlideTex形式で作成したファイルを読み込み，
PowerPointスライドを作成します．

SlideTexの内容をもとに，スライドの構成やデザインを反映した
PowerPointファイルを作成できます．
""",
        ),

        # --------------------------------------------------------
        # 基本的な流れ
        # --------------------------------------------------------

        GuideSection(
            title="基本的な流れ",
            body="""
<div class="guide-point">
<span class="guide-route">SlideTexファイル ➡ 内容を解析 ➡ PowerPointスライド</span><br>
</div>
""",

            subsections=(

                # ------------------------------------------------
                # ① SlideTexファイルを設定する
                # ------------------------------------------------

                GuideSubSection(
                    title="① SlideTexファイルを設定する",
                    body="""
作成したSlideTexファイルをアップロードします．

<div class="guide-note">
<span class="guide-route">ポータル ▶ テキストスタジオ ▶ slideTex</span><br>
</div>
""",
                ),

                # ------------------------------------------------
                # ② 解析結果を確認する
                # ------------------------------------------------

                GuideSubSection(
                    title="② 解析結果を確認する",
                    body="""
SlideTexファイルの解析結果を確認します．

構文エラーが表示された場合は，
SlideTexファイルを修正して再度読み込みます．
""",
                ),

                # ------------------------------------------------
                # ③ PowerPointを作成する
                # ------------------------------------------------

                GuideSubSection(
                    title="③ PowerPointを作成する",
                    body="""
出力するファイル名を確認し，【PowerPointを作成】を押します．
""",
                ),

                # ------------------------------------------------
                # ④ PowerPointを保存する
                # ------------------------------------------------

                GuideSubSection(
                    title="④ PowerPointを保存する",
                    body="""
作成されたPowerPointファイルをダウンロードして保存します．
""",
                ),

                # ------------------------------------------------
                # ⑤ 作成結果を確認する
                # ------------------------------------------------

                GuideSubSection(
                    title="⑤ 作成結果を確認する",
                    body="""
<div class="guide-red">
担当者は主に次の作業を行います．

- スライドの構成や内容が意図どおりになっているか確認する．
- レイアウトや文字の表示に問題がないか確認する．
- 必要に応じてSlideTexファイルを修正し，再度PowerPointを作成する．
</div>
""",
                ),

                # subsectionsのクローズ
            ),

            # GuideSectionのクローズ
        ),

        # sectionsのクローズ
    ),

    # GuideItemのクローズ
)


# ============================================================
# 大項目：文書・資料の作成
# ============================================================

GUIDE_DOCUMENT_CREATION = GuideCategory(
    key="document_creation",
    title="文書・資料の作成",
    icon="✍️",
    summary="文章，メール文，表，スライドなど，業務で使用する文書や資料を作成します．",
    items=(
        ITEM_AI_WRITING,
        ITEM_TABLE_CREATION,
        ITEM_SLIDE_CREATION,
    ),
)
